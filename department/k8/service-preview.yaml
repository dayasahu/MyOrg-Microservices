apiVersion: v1
kind: Service
metadata:
  name: department-preview
  labels:
    app: department
    role: preview
spec:
  selector:
    app: department
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
