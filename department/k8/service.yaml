apiVersion: v1
kind: Service
metadata:
  name: department
spec:
  selector:
    app: department
  type: ClusterIP
  ports:
    - port: 80          # 👈 STANDARD SERVICE PORT
      targetPort: 8080
      protocol: TCP
